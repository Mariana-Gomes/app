// Arrays, objects

let meta = {
  value: "Ler um livro por mês",
  checked: false,
  isChecked: () => {},
};

console.log(meta.isChecked());

// Function // Arrow Function

const criarMeta = () => {};

// function criarMeta() {}
